package logger

const (
	// InfoLevel traces just as information
	InfoLevel = "info"

	// ErrorLevel indicates a trace is an error
	ErrorLevel = "error"

	// WarningLevel indicates a trace is an error
	WarningLevel = "warning"
)
